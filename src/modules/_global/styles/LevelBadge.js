import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
  },
  badge: {
    borderWidth: 2,
    borderColor: 'black',
    backgroundColor: 'red',
    fontSize: 20
  },
  text: {
    color: 'orange',
    fontSize: 11,
  }
});

export default styles;
