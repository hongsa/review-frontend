import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  reviewCardTitle: {
    color: '#f93854',
    fontWeight: 'bold'
  },
  reviewCardBar: {
    color: '#b0b0b0'
  },
  reviewCardAuthor: {
    color: 'black',
    fontSize: 12
    // fontWeight: 'bold'
  }
});

export default styles;
